version: "3"

services:
  test:
    build:
      context: ./
      dockerfile: Dockerfile-latest
    environment:
      - "UID=1000"
      - "GID=1000"
    ports:
      - "127.0.0.1:8888:80"
    hostname: test.mysite
